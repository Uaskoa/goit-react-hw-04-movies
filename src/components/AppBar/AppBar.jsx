import Navigation from "../Navigation/Navigation";

import "./AppBar.scss";

const AppBar = () => {
  return (
    <header className="header">
      <Navigation />
    </header>
  );
};

export default AppBar;
